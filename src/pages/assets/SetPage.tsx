import { TopNav } from "@/components";
import * as S from "./SetPage.styles";

export default function SetPage() {
  return (
    <S.SetPageContainer>
      <TopNav />
      {/* 나머지 컴포넌트*/}
    </S.SetPageContainer>
  );
}
